{
  "private": true,
  "scripts": {
    "server": "hexo server",
    "generate": "hexo clean && hexo generate",
    "deploy": "node prepare.js && hexo clean && hexo generate && hexo deploy && rm -rf .deploy_git"
  },
  "hexo": {
    "version": "3.7.1"
  },
  "dependencies": {
    "fluide": "git+https://git@github.com/nerdslabs/fluide.git",
    "hexo": "^3.7.1",
    "hexo-deployer-git": "^0.3.1",
    "hexo-generator-index": "^0.2.1",
    "hexo-renderer-ejs": "^0.3.1",
    "hexo-renderer-marked": "^0.3.2",
    "hexo-renderer-sass": "^0.3.2",
    "hexo-renderer-stylus": "^0.3.3",
    "hexo-server": "^0.3.2"
  },
  "devDependencies": {
    "gzip-size": "^4.1.0"
  }
}
